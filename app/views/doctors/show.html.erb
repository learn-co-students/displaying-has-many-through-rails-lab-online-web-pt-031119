<h2><%= @doctor.name %>'s Appointments:</h2>

<% @doctor.appointments.each do |appt| %>
	<p>Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %> Appointment Time:  <%= appt.appointment_datetime.strftime('%B %d, %Y') %> at <%= appt.appointment_datetime.strftime('%H:%M') %></p>
	<% end %>