<h1><%=@doctor.name%></h1>

<h2><% @doctor.appointments.each do |a|%></h2>
<p><%= a.appointment_datetime.to_formatted_s(:long_ordinal) %></p>
<p><%= link_to a.patient.name, patient_path(a.patient) %></p>
<%end%>
