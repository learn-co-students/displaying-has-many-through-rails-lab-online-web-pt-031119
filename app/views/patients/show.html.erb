<h2><%= @patient.name %></h2>

 <h4>Appointments:</h4>
<ul>
<% @patient.appointments.each do |appt| %>
    <li> <%= appt.appointment_datetime.strftime('%B %d, %Y at %H:%M') %> with <%= link_to "Dr. #{appt.doctor.name}", doctor_path(appt.doctor) %></li>
<% end %>
</ul>