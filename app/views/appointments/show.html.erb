<h1><%= @appointment.appointment_datetime.strftime("%B %d, %Y at %I") %></h1>
<p><%= link_to @appointment.patient.name, patient_path(@appointment.patient) %></p>
<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %>